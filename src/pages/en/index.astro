---
/* Versão em inglês da landing: mesma página, texto de `src/i18n/landing.ts`. As rotas de origem
 * (`/de/<ferramenta>`) continuam só em português — são os links das telas da Nipo. */
import Landing from '../../layouts/Landing.astro';
---

<Landing idioma="en" />
